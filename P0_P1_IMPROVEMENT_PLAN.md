# 🚀 LingoFlow 功能完善与体验升级方案 (P0 & P1)

> **文档定位**：面向产品决策与后续落地的功能改造指引与技术路线图。  
> **编写原则**：全篇采用通俗易懂的自然语言，严禁生涩技术黑话；既讲清**“为什么改、改完后用户感知到什么”**，又精确指出**“需要修改的文件与对应代码位置”**，确保后续开发或 AI 助理能够照单执行、无缝交付。

---

## 🎯 方案总体规划与目标

根据早前的功能走查，本次升级聚焦在体验提升最明显、性价比最高的两个阶段：

```
┌─────────────────────────────────────────────────────────────┐
│  阶段一：P0 体验质变期（消除所有断头路与半成品逻辑）             │
│  目标：把现有的核心流程跑通，不再有死胡同、假数据和逻辑硬伤     │
├─────────────────────────────────────────────────────────────┤
│  1. 口语收藏一键直达生词本 + 词条详情完整补全                   │
│  2. 彻底解决闪卡“背完硬塞 5 个词”的体验，给予掌控感与成就感     │
│  3. 测验答题后生成真实成绩单，做错的词自动打回“重新背”         │
│  4. 修复长难句被英文缩写（Mr./U.S./e.g.）腰斩截断的问题        │
└─────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段二：P1 核心护城河期（媲美主流商业工具的顺滑感）             │
│  目标：从“能用”跨越到“好用”，大幅降低使用门槛与等待焦虑       │
├─────────────────────────────────────────────────────────────┤
│  5. 口语对话升级为“打字机流式输出”，告别几秒十几秒的傻等转圈   │
│  6. 阅读器支持“一键粘贴网页链接提取文章”与“整段双语对照”       │
│  7. 引入真正聪明自适应的艾宾浩斯复习算法 + 每日打卡激励统计     │
└─────────────────────────────────────────────────────────────┘
```

---

## 一、P0 阶段方案：消除功能断点与体验硬伤

### 任务 1：口语对练“收藏直达”与生词数据完整化

#### 1. 现有问题
- 用户在口语纠错面板中点击“存入生词本”后，虽然显示了“已收藏”，但没有通往生词本的快捷入口，原本设计好的跳转通道在代码里被遗忘了。
- 存入的生词缺少音标、英文释义，甚至连例句的中文翻译都是空的，进入生词卡后背面空空如也。

#### 2. 改动后的用户体验
- 点击“存入生词本”后，按钮旁边多出一个 **“查看生词卡 →”** 的轻便按钮，点击直接无缝跳转到生词本界面。
- 存入生词本时，系统后台自动调用词典分析或复用纠错面板里的中英文含义，把**音标、词性、例句中文翻译**全套补齐，保证生词卡翻过来时信息丰满完整。

#### 3. 对应代码修改位置与方案
- **涉及文件**：`src/components/OralCoach.jsx`
  - **位置**：第 23 行接收的 `onNavigateToVocab` 回调函数，在第 455 行左右的“已收藏”反馈区域接入点击事件：
    ```jsx
    {addedWordFeedback[msg.feedback.betterAlternative] ? (
      <button 
        onClick={onNavigateToVocab}
        className="flex items-center gap-1 text-sky-700 bg-sky-50 px-2 py-1 rounded-md text-[11px] font-semibold hover:bg-sky-100 transition-colors"
      >
        <span>✅ 已收藏，去生词本查看 →</span>
      </button>
    ) : ( ... )}
    ```
  - **位置**：第 225 行 `handleAddExpressionToVocab` 函数，将收藏词条的逻辑完善：自动异步调用 AI 分析或利用当前纠错内容填入 `contextSentenceCn`，彻底告别空内容。

---

### 任务 2：生词闪卡“今日背完”彻底清爽结算

#### 1. 现有问题
- 用户辛辛苦苦把今天到期的生词都复习完了，只要刷新或重新点开页面，系统发现“今天没有词要背了”，就会自作主张地从词库头部抓出 5 个词硬塞给用户，导致用户永远看不到“任务已完成”的清爽状态。

#### 2. 改动后的用户体验
- 当今天的所有卡片复习完成后，界面展示一个设计精致的**“今日任务已全部搞定！”**打卡荣誉卡。
- 明确告诉用户：“今天没有需要复习的生词了！下一次复习将在明天。”
- 如果用户还想继续学，提供两个明确的主动选项：**“随便翻翻已掌握的词”** 或 **“去精读里收割几个新词”**，把选择权完全还给用户。

#### 3. 对应代码修改位置与方案
- **涉及文件**：`src/components/VocabularySRS.jsx`
  - **位置**：第 58~63 行的初始化待复习列表逻辑：
    - **修改前**：
      ```javascript
      const due = words.filter((w) => !w.nextReviewDate || w.nextReviewDate <= now + 60 * 60 * 1000);
      setDueCards(due.length > 0 ? due : words.slice(0, 5)); // 强行塞5个词的元凶
      ```
    - **修改后**：
      ```javascript
      const due = words.filter((w) => !w.nextReviewDate || w.nextReviewDate <= now + 60 * 60 * 1000);
      setDueCards(due); // 只放真正需要复习的词
      if (due.length === 0 && words.length > 0) {
        setReviewCompleted(true); // 直接呈现今日已打卡完成状态
      }
      ```
  - **位置**：在背完的结算视图中，区分“刚刚背完全部”与“今天原本就没有到期待复习的词”，展示更温馨、更具掌控感的打卡提示。

---

### 任务 3：AI 巩固测验成绩单与错题自动打回重背机制

#### 1. 现有问题
- 用户做完 3 道题后，没有任何结算页面；做错的词依然留在生词本里，艾宾浩斯复习周期毫不知情，做题纯属一次性娱乐，没有学习闭环。

#### 2. 改动后的用户体验
- **做完 3 题后弹出结算成绩卡**：比如“恭喜！本次测验得分 2/3 分（正确率 67%）”。
- **错题自动反哺生词本**：对于做错的那道题，系统自动在该单词的标签里打上`【测验薄弱】`印记，并**立刻将该单词的状态重置为“遗忘（今天需再次复习）”**，自动加回当天的待复习闪卡队伍中，实现“哪里薄弱立刻补哪里”。

#### 3. 对应代码修改位置与方案
- **涉及文件**：`src/components/VocabularySRS.jsx`
  - **位置**：激活原先被闲置的 `quizScore` 状态，每当用户做完最后一题（例如第 3 题点击选项后），遍历 `selectedAnswers` 计算总得分与错题列表。
  - **位置**：对答错的题，调用 `StorageService.updateWordSRS(wrongWordId, 'again')`，让系统自动将该词的复习周期回退到第 1 天。
  - **位置**：在列表底部新增“成绩结算弹窗”，展示本次得分、错题解析清单以及“把错题立刻加入闪卡强化复习”的快捷按钮。

---

### 任务 4：外刊长难句分句规则强化（防腰斩断句）

#### 1. 现有问题
- 目前分句采用简单的正则表达式（遇到句号、感叹号、问号就切断），只要文章中遇到常见外刊缩写（如 `e.g.` 例如、`i.e.` 即、`Mr.` 先生、`U.S.` 美国），句子就会被强行切碎。长难句解析拿到的往往是半截残废句子。

#### 2. 改动后的用户体验
- 无论英文段落里出现多少人物头衔、机构名称或拉丁缩写，系统都能像真正的英语母语老师一样，精准识别出整句的起始点，长难句剖析 100% 针对完整语义展开。

#### 3. 对应代码修改位置与方案
- **涉及文件**：`src/components/SmartReader.jsx`
  - **位置**：第 87 行与第 269 行的分句逻辑：
    - 将纯正则切分改造为**“防缩写保护的分句算法”**：利用现代手机浏览器原生支持的国际化智能断句器 `Intl.Segmenter('en', { granularity: 'sentence' })`，精准切分整句；同时兼顾老旧设备的缩写词保护。

---

## 二、P1 阶段方案：打磨核心，形成使用护城河

### 任务 5：口语对话“流式打字机响应”（告别死等转圈）

#### 1. 现有问题
- 用户通过语音或文字发出一句话后，大模型需要在后台生成一大段包含纠错、建议、回复的 JSON 数据。在这几秒甚至十几秒里，界面只有一个小圆点转圈，用户完全不知道 AI 有没有在听，体验非常焦虑且容易误以为死机。

#### 2. 改动后的用户体验
- 像使用 ChatGPT、Claude 或微信打字一样，用户发送完毕后 **0.5 秒内**，外教 Echo 的回复内容像水龙头出水一样**逐字流畅吐出**（打字机效果）。
- 说话与纠错分层展示：正文回复优先打字输出，底部的“语法纠错与高阶升级”面板在生成完成后优雅淡入，交互行云流水。

#### 3. 对应代码修改位置与方案
- **涉及文件**：
  - `src/services/ai.js`：新增 `callAICompletionStream` 流式调用函数，利用浏览器原生的流式数据通道（ReadableStream），每收到大模型吐出的一个字就立即触发界面刷新。
  - `src/components/OralCoach.jsx`：在消息列表中增加“打字机流式渲染”机制，让文字逐字跃然屏上，彻底消除转圈等待感。

---

### 任务 6：智能精读支持“网页外刊一键提取”与“整段双语对照”

#### 1. 现有问题
- 自学英语的人每天都在读各种公众号英文文章、BBC/经济学人外网新闻或推特长文，目前只能手动一点点复制粘贴，排版杂乱，极其繁琐。
- 遇到一段难度较高的文本，虽然能单句剖析，但无法一键看整段的中文大意对照，阅读连贯性被打断。

#### 2. 改动后的用户体验
- **网页链接一键抓取**：导入文章时，新增一个【输入网页网址】输入框。粘贴一篇英文外刊链接（例如 BBC、Medium、CNN），系统自动提取出干净的标题和正文段落，一键导入阅读器。
- **逐段双语对照开关**：在文章顶部增加一个“双语对照”开关，点击段落旁的“译”字图标，即可直接在英文段落下方展开整段优雅译文。

#### 3. 对应代码修改位置与方案
- **涉及文件**：
  - `src/components/SmartReader.jsx`：
    - 在“导入文章”弹窗中增加“粘贴文章网址”提取选项。
    - 针对段落增加“段落翻译缓存与切换”状态，点击段落译文按钮即时调用大模型翻译整段并缓存。

---

### 任务 7：艾宾浩斯动态自适应算法与每日学习打卡统计

#### 1. 现有问题
- 当前代码里的记忆衰减乘数始终是写死的 `2.5`，背容易的词和背超级难的词延长时间一模一样，并不是真正的科学记忆曲线。
- 软件没有任何学习数据记录，自学者坚持了多少天、背了多少个单词全凭记忆，缺乏坚持下去的心理激励。

#### 2. 改动后的用户体验
- **真正的聪明记忆曲线**：背一个词时，如果连续选择“模糊”，系统会自动降低该词的记忆乘数（缩短下次碰面时间）；如果连续选择“轻松掌握”，系统会自动加大延长时间，真正把宝贵的精力留给最顽固的生词。
- **每日打卡与成就徽章**：生词本顶部新增**“🔥 已连续学习 X 天”**、**“累计掌握 X 词”**的精美数据胶囊，每天第一次完成闪卡复习后，触发专属的今日打卡成功徽章。

#### 3. 对应代码修改位置与方案
- **涉及文件**：
  - `src/services/storage.js`：完善 `updateWordSRS` 逻辑，正式接入标准 SuperMemo SM-2 动态难度因数计算公式（让 `easeFactor` 随着用户的点击选择在 1.3 ~ 2.5 之间真实动态浮动）。
  - `src/services/storage.js`：在本地存储中新增“每日学习打卡记录表（记录日期、已复习数、对练轮次）”。
  - `src/components/VocabularySRS.jsx`：在顶部 Header 区域直观呈现打卡天数与复习统计徽章。

---

## 三、文件修改清单与风险控制

| 优先级 | 涉及的核心源文件 | 改动性质 | 预期风险与防护措施 |
| :--- | :--- | :--- | :--- |
| **P0** | `src/components/OralCoach.jsx` | 交互跳转与参数完善 | **零风险**：仅补全现有未接通的回调与字段填充。 |
| **P0** | `src/components/VocabularySRS.jsx` | 逻辑修复与成绩弹窗 | **极低风险**：纠正待复习卡片过滤逻辑，增加测验结果闭环。 |
| **P0** | `src/components/SmartReader.jsx` | 分句算法加固 | **零风险**：采用浏览器原生 `Intl.Segmenter`，对已有文章无破坏性。 |
| **P1** | `src/services/ai.js` | 增加流式接口封装 | **低风险**：保留原有非流式函数，新增流式函数，向下兼容。 |
| **P1** | `src/components/OralCoach.jsx` | 接入打字机实时展示 | **低风险**：做好流式传输异常断开时的错误兜底。 |
| **P1** | `src/components/SmartReader.jsx` | 增加段落对照翻译与外链解析 | **中低风险**：外链解析需优雅处理跨域提示或提供干净正文粘贴回退。 |
| **P1** | `src/services/storage.js` | 算法升级与打卡数据结构扩充 | **极低风险**：新老数据结构自动做平滑合并，绝不影响用户原有生词库。 |

---

## 四、非程序员也能轻松验收的【测试清单】

改完之后，你可以打开网页按照以下步骤逐一验证：

- [ ] **验证 1（口语收藏直达）**：在口语聊天中输入一句带有小语病的英文，等待纠错面板出现，点击“存入生词本”，看旁边是否出现**“✅ 已收藏，去生词本查看 →”**，点击后是否能立刻跳到生词本界面看到完整的音标与中文意思。
- [ ] **验证 2（闪卡清零体验）**：把今天待复习的闪卡全部划完，页面是否弹出一张清爽的“今日任务已全部搞定”卡片？再次刷新网页，是否**不会**再硬塞 5 个旧词进来？
- [ ] **验证 3（测验做错打回）**：去“AI 巩固测验”做 3 道题，故意选错 1 题，答完后是否弹出了成绩单（例如 2/3 分）？回到生词本列表，刚才做错的那个单词是否被自动打上了需重温的标记并回到了待复习列表？
- [ ] **验证 4（缩写分句不腰斩）**：在阅读器里导入一段包含 `Dr. Smith graduated from the U.S.` 的短文，看句子是不是一整句连在一起，而不是被截断成 `Dr.` 一半、后面一半。
- [ ] **验证 5（口语打字机流式感受）**：在口语对话里发送一句话，外教的回应是不是一秒内开始逐字冒出来，而不是以前那种傻等十几秒一次性弹出一大块？
